# 安全说明

- API Key 只保存在宿主进程内存与 `<profileDir>/dsh-wallet-switcher/settings.json`（本机用户目录），浏览器 `localStorage` 不保存任何凭据；升级时会自动清除 0.2.x 遗留的网页登录令牌。
- 浏览器只能拿到查询结果（余额、今日用量、打码 Key），拿不到完整 Key；配置接口的响应也不会回显 Key。
- 余额 URL 只允许公网 HTTPS：`http`、本机、内网段（127/8、10/8、172.16/12、192.168/16、169.254/16 等）与带用户名密码的 URL 一律拒绝，避免宿主进程被当成 SSRF 跳板。
- 0.2.x 的网页登录令牌已不再支持；令牌若已在聊天或截图中暴露，退出对应网页重新登录即可使其失效。
- 不要把 API Key 提交到 Git、截图或聊天记录；Key 泄露后请到服务商面板重置。
