- insert:
    - id: dsh-wallet-switcher
      name: '@dsh-external/dsh-wallet-switcher'
      config:
        balanceUrl: !!js process.env.DSH_WALLET_BALANCE_URL
        accessToken: !!js process.env.DSH_WALLET_ACCESS_TOKEN
